def document
    (word
        (lang-en "document")
        (lang-ru "документ"))
    (!-- " point on time line (this element is internal on that line, because zoom quite big) ")
    (timestamp)
    (place).
