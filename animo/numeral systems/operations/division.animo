def division
    (binary-operation)
    (word
        (lang-en "division")
        (lang-ru "деле́ние")
        (!-- " операция деления "))
    (denoted "÷")
    (wiki
        uri "http://en.wikipedia.org/wiki/Division_(mathematics)")
    (!-- "
	a / b = c 
	a is called def dividend, b def divisor and c def quotient.
	")
    (!-- " 
	Сколько раз 3 содержится в 14?
	14 называется делимым, число 3 — делителем, число 4 — (неполным) частным и число 2 — остатком (от деления)
	 ").
