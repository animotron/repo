def rational-number
    (word
        (lang-en "rational number")
        (lang-ru "рациональное число")
        (!-- " TODO: <have:lang-la>ratio — отношение, деление, дробь</have:lang-la> "))
    (denoted "ℚ")
    (!-- " TODO: boldface Q ")
    (wiki
        uri "http://en.wikipedia.org/wiki/Rational_number")
    (!-- " 
	any number that can be expressed as def quotient or fraction a/b of two integers,
	with def denominator b not equal to zero
	").
