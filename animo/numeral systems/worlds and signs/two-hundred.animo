def two-hundred
    (!-- " TODO: remove? ")
    (digit)
    (natural-number)
    (word
        (lang-en "two hundred")
        (!-- " TODO: two + \" \" +hundred ")
        (lang-ru "двести")
        (!-- " TODO: две + сти ")
        (!-- " TODO: Синоним: две сотни ")
        (!-- " TODO: двухсот, двумстам, двумястами, о двухстах ")
        (!-- " TODO: поряд. двухсотый, -ая, -ое "))
    (denoted 200)
    (!-- " десятичная система счисления ").
