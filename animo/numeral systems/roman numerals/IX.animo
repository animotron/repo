def IX
    (digit)
    (!-- " TODO correct ??? ")
    (roman-numerals)
    (!-- " 
    <have:name>
		<ptrn:language>
            <have:lang-la></have:lang-la>
        </ptrn:language>
	</have:name>
	")
    (denoted "IX")
    (!-- " TODO: <have:natural-number>9</have:natural-number> ")
    (!-- " TODO: IX == nine ? ").
